{
    "token": "${TOKEN}",
    "client_id": "${CLIENT_ID}",
    "client_secret": "${CLIENT_SECRET}",
    "redirect_uri": "${REDIRECT_URI}",
    "scope": "${SCOPE}",

    "admin_guilds": [${ADMIN_GUILDS}],
    "owners": [${OWNERS}],
    "log_channel": ${LOG_CHANNEL},

    "server_host": "${SERVER_HOST}",
    "server_port": ${SERVER_PORT},

    "verify_guilds": {
        "1111870619814998066": 11153125705811230821,
        "Put your server ID here": "Put your role ID for this server here (without quotes). You can leave this dict empty for no guilds with verification system."
    },

    "server_logging": ${SERVER_LOGGING},
    "available-scopes": "${AVAILABLE_SCOPES}"
}
